# ميزة بيع وإدارة الدومينات 🌐

## نظرة عامة
تم إضافة نظام شامل لبيع وإدارة الدومينات في منصة باكورة أعمال، يتيح للمستخدمين البحث عن الدومينات وشرائها وإدارتها بسهولة.

## الميزات المضافة ✨

### 1. البحث عن الدومينات
- **المسار**: `/domains/search`
- **الميزات**:
  - بحث متقدم عن الدومينات المتاحة
  - دعم امتدادات متعددة (.com, .net, .org, .sa, .com.sa, .info, .biz)
  - عرض الأسعار مباشرة
  - اقتراحات بديلة للدومينات غير المتاحة
  - إضافة للسلة

### 2. إدارة الدومينات
- **المسار**: `/portal/domains`
- **الميزات**:
  - عرض جميع الدومينات المملوكة
  - إحصائيات شاملة (نشط، قارب على الانتهاء، منتهي)
  - تجديد الدومينات
  - تفعيل/إيقاف التجديد التلقائي
  - ربط الدومين بالمواقع
  - إدارة DNS
  - فلاتر متقدمة

### 3. إدارة DNS
- **المكون**: `DNSManager`
- **الميزات**:
  - إضافة/تعديل/حذف سجلات DNS
  - دعم جميع أنواع السجلات (A, AAAA, CNAME, MX, TXT, NS)
  - قوالب جاهزة (موقع ويب، بريد إلكتروني، Cloudflare)
  - إعدادات TTL مرنة
  - واجهة سهلة الاستخدام

## البنية التقنية 🏗️

### قاعدة البيانات
تم إنشاء الجداول التالية:

1. **domains** - معلومات الدومينات
2. **dns_records** - سجلات DNS
3. **domain_transactions** - المعاملات المالية
4. **domain_pricing** - أسعار الامتدادات
5. **domain_notifications** - إشعارات الدومينات
6. **websites** - المواقع المرتبطة

### المكونات (Components)

```
src/components/platform/
├── DomainSearch.js          # البحث عن الدومينات
├── DomainManagement.js      # إدارة الدومينات
└── DNSManager.js            # إدارة DNS
```

### Routes الجديدة

```javascript
/domains/search          // البحث عن دومين
/portal/domains          // إدارة الدومينات
```

## التثبيت والإعداد 🚀

### 1. تطبيق Schema قاعدة البيانات

```bash
# في Supabase SQL Editor، قم بتشغيل:
supabase_domains_schema.sql
```

### 2. إضافة متغيرات البيئة

أضف المتغيرات التالية في ملف `.env`:

```env
# Namecheap API (للإنتاج)
REACT_APP_NAMECHEAP_API_USER=your_api_user
REACT_APP_NAMECHEAP_API_KEY=your_api_key
REACT_APP_NAMECHEAP_USERNAME=your_username
REACT_APP_NAMECHEAP_CLIENT_IP=your_whitelisted_ip

# Moyasar Payment
REACT_APP_MOYASAR_API_KEY=your_api_key
REACT_APP_MOYASAR_PUBLISHABLE_KEY=your_publishable_key

# Backend URL (إذا كان لديك backend منفصل)
REACT_APP_BACKEND_URL=http://localhost:5000
```

### 3. تشغيل المشروع

```bash
npm install
npm start
```

## استخدام الميزة 📖

### للمستخدمين

1. **البحث عن دومين**:
   - انتقل إلى `/domains/search`
   - أدخل اسم الدومين المطلوب
   - اختر الامتدادات المرغوبة
   - اضغط "بحث"

2. **شراء دومين**:
   - اختر الدومين المتاح
   - اضغ "إضافة للسلة"
   - أكمل عملية الدفع

3. **إدارة الدومينات**:
   - انتقل إلى `/portal/domains`
   - عرض جميع دوميناتك
   - تجديد أو إدارة DNS

4. **إدارة DNS**:
   - من صفحة إدارة الدومينات
   - اضغط "إدارة DNS"
   - أضف أو عدل السجلات

### للمطورين

#### إضافة سجل DNS برمجياً

```javascript
import { supabase } from './utils/supabaseClient';

const addDNSRecord = async (domainId, record) => {
  const { data, error } = await supabase
    .from('dns_records')
    .insert([{
      domain_id: domainId,
      record_type: 'A',
      host: '@',
      value: '192.0.2.1',
      ttl: 3600
    }]);
  
  if (error) throw error;
  return data;
};
```

#### التحقق من توفر دومين

```javascript
const checkDomainAvailability = async (domainName, extension) => {
  const fullDomain = domainName + extension;
  
  const { data } = await supabase
    .from('domains')
    .select('id')
    .eq('full_domain', fullDomain)
    .single();
  
  return !data; // true if available
};
```

## Row Level Security (RLS) 🔒

تم تطبيق سياسات أمان صارمة:

- المستخدمون يرون دوميناتهم فقط
- الأدمن يمكنهم رؤية وإدارة جميع الدومينات
- سجلات DNS محمية حسب ملكية الدومين
- المعاملات المالية خاصة بكل مستخدم

## التكاملات المستقبلية 🔮

### المرحلة الثانية (قريباً)

- [ ] تكامل Namecheap API الفعلي
- [ ] بوابة الدفع (Moyasar/Stripe)
- [ ] نقل الدومينات من مزودين آخرين
- [ ] WHOIS Privacy
- [ ] Email Forwarding
- [ ] سوق إعادة بيع الدومينات
- [ ] دومينات Premium

### Backend API المطلوب

```javascript
// مثال على endpoints المطلوبة
POST   /api/domains/check           // التحقق من التوفر
POST   /api/domains/purchase        // شراء دومين
POST   /api/domains/renew/:id       // تجديد دومين
PUT    /api/domains/dns/:id         // تحديث DNS
POST   /api/domains/transfer        // نقل دومين
GET    /api/domains/pricing         // الأسعار
```

## الإشعارات التلقائية 🔔

تم إنشاء دالة SQL لإرسال إشعارات تلقائية:

```sql
-- يتم استدعاؤها دورياً (Cron Job)
SELECT create_domain_expiry_notification();
```

الإشعارات تُرسل:
- 30 يوم قبل الانتهاء
- 15 يوم قبل الانتهاء
- 7 أيام قبل الانتهاء
- يوم الانتهاء

## الأسعار الافتراضية 💰

| الامتداد | السعر السنوي |
|---------|--------------|
| .com    | 50 ر.س      |
| .net    | 55 ر.س      |
| .org    | 60 ر.س      |
| .sa     | 150 ر.س     |
| .com.sa | 100 ر.س     |
| .info   | 45 ر.س      |
| .biz    | 50 ر.س      |

## الاختبار 🧪

### اختبار يدوي

1. **البحث**:
   - ✅ البحث عن دومين متاح
   - ✅ البحث عن دومين غير متاح
   - ✅ عرض الاقتراحات
   - ✅ إضافة للسلة

2. **الإدارة**:
   - ✅ عرض قائمة الدومينات
   - ✅ الفلاتر (نشط، قارب على الانتهاء، منتهي)
   - ✅ تفعيل التجديد التلقائي
   - ✅ ربط بموقع

3. **DNS**:
   - ✅ إضافة سجل A
   - ✅ إضافة سجل CNAME
   - ✅ إضافة سجل MX
   - ✅ حذف سجل
   - ✅ استخدام القوالب

### اختبار آلي (قريباً)

```javascript
// مثال على اختبار
describe('Domain Search', () => {
  it('should search for available domains', async () => {
    // Test implementation
  });
});
```

## الأداء والتحسينات ⚡

- استخدام Lazy Loading للمكونات
- Caching لنتائج البحث
- Pagination لقوائم الدومينات الكبيرة
- Debouncing للبحث
- Optimistic UI Updates

## الدعم الفني 💬

للمساعدة أو الإبلاغ عن مشاكل:
- البريد الإلكتروني: oooomar123450@gmail.com
- GitHub Issues: [رابط المشروع]

## المساهمة 🤝

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء branch جديد
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

## الترخيص 📄

هذا المشروع مرخص تحت [LICENSE](../LICENSE)

## الشكر والتقدير 🙏

- فريق Supabase
- مجتمع React
- Namecheap API
- Moyasar Payment Gateway

---

**ملاحظة**: هذه الميزة في مرحلة MVP. التكاملات الخارجية (Namecheap API، Moyasar) تحتاج إلى تفعيل في بيئة الإنتاج.

**تاريخ الإصدار**: 2026-01-13
**الإصدار**: 1.0.0
**الحالة**: MVP Ready ✅
