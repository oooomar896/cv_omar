# التصميم الهندسي للنظام (System Architecture) - AI Project Builder

يصف هذا المستند البنية التحتية، تدفق البيانات، ومنطق توزيع المهام بين وكلاء الذكاء الاصطناعي لضمان بناء نظام متكامل وعالي الأداء.

---

### 🏛️ 1. المخطط العام (High-Level Architecture)

النظام يعتمد على بنية **Client-Server-Agentic**:
- **Frontend:** React.js (مع Tailwind CSS و Framer Motion) لتوفير تجربة مستخدم سلسة.
- **Backend:** Node.js (Express/NestJS) لإدارة الطلبات، المدفوعات، والجلسات.
- **AI Core:** Multi-Agent Orchestrator يعتمد على LangGraph أو نظام Custom لربط الوكلاء.
- **Infrastructure:** Supabase (Auth, DB, Storage) + BullMQ للمعالجة الخلفية.

---

### 🗄️ 2. مخطط قاعدة البيانات (Database Schema)

| الجدول (Table) | الحقول الأساسية (Key Fields) | الوصف |
| :--- | :--- | :--- |
| **Users** | `id, email, full_name, credits` | بيانات المستخدمين ونظام الرصيد. |
| **Orders** | `id, user_id, service_type, status, payment_id` | تتبع حالة المشروع (قيد المعالجة، مكتمل). |
| **ProjectMetadata** | `id, order_id, idea_description, tech_stack, features` | البيانات المستخلصة من نموذج الإدخال. |
| **AI_Outputs** | `id, order_id, agent_name, content, version` | 저장 المخرجات الوسيطة لكل Agent (تحليل، كود، إلخ). |

---

### 🤖 3. منطق الوكلاء (Multi-Agent Orchestration)

يعمل النظام عبر "سيمفونية" من الوكلاء يتواصلون عبر حالة مشتركة (Shared State):

1.  **Orchestrator (The Leader):**
    - يستقبل وصف المستخدم.
    - يوجه المهمة للوكيل المناسب.
    - يتحقق من جودة مخرجات كل مرحلة قبل الانتقال للتالية.

2.  **Strategic Agent (Analysis):**
    - يجري بحثاً استنتاجياً في السوق.
    - يولد ملف `business_plan.md` و `features_list.json`.

3.  **Architect Agent (Design):**
    - يقرأ قائمة الميزات.
    - يحدد أفضل الأدوات (مثلاً: Next.js + Prisma).
    - يولد الـ `database_schema.sql`.

4.  **Coder Agent (Execution):**
    - يولد هيكل المجلدات.
    - يكتب الكود الأساسي (Boilerplate).
    - يدمج التوثيق البرمجي.

5.  **QA Agent (Review):**
    - يقوم بعمل `Code Review`.
    - يتأكد من تطابق المخرجات مع طلب المستخدم الأصلي.

---

### 🔄 4. تدفق البيانات (Data Flow)

1.  **Input:** المستخدم يدخل وصف الفكرة -> يتم استخلاص الميزات أولياً.
2.  **Check:** التحقق من نجاح الدفع (Webhook).
3.  **Queue:** إضافة المهمة إلى طابور المعالجة (Redis).
4.  **Process:** سحب المهمة من قبل الـ Orchestrator وتفعيل الوكلاء بالتسلسل.
5.  **Output:** ضغط النتائج في ملف ZIP -> رفع الرابط للـ Storage -> إرسال إشعار للمستخدم.

---

### 🛠️ 5. المكونات التقنية المختارة (Tech Stack)

- **AI Library:** OpenAI NodeJS SDK + LangChain.
- **State Management:** Zustand (للنماذج المعقدة).
- **Styling:** Tailwind CSS (Dark Mode optimization).
- **Communication:** WebSockets (Socket.io) لتحديث المستخدم بحالة التوليد لحظياً.

---

### 🛡️ 6. معايير الأمان (Security & Optimization)

- **Rate Limiting:** منع إساءة استخدام واجهة الـ AI.
- **Prompt Isolation:** كل مشروع له Context معزول تماماً.
- **Code Sanitization:** فحص الكود المولد للتأكد من عدم وجود ثغرات أمنية (Injections).
